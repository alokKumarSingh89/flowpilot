# Public, non-secret configuration for the API bootstrap.
# NODE_ENV is required and must be development, test, or production.
NODE_ENV=development
# API_PORT is required and must be an integer from 1 through 65535.
API_PORT=3000
# Optional service identifier used only in sanitized diagnostics.
SERVICE_NAME=api
